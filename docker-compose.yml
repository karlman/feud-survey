services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: feud
      POSTGRES_PASSWORD: feud
      POSTGRES_DB: feud_survey
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
